# JAEN v2.1.0 復旧手順書 🚀

## ⚠️ 緊急復旧手順

### 問題: `npm start` で "Missing script: start" エラー

**原因:** package.jsonに起動スクリプトが定義されていない

**解決手順:**

1. **package.json修正**
```bash
# package.json の scripts セクションに追加
"scripts": {
  "start": "node index.js",
  "dev": "node index.js",
  "test": "echo \"Error: no test specified\" && exit 1"
}
```

2. **依存関係確認**
```bash
npm install
```

3. **環境変数確認**
```bash
# .envファイルが存在し、以下が設定されているか確認
BOT_TOKEN_VALUE=your_discord_bot_token
TRANSLATE_API_KEY=your_google_api_key
```

4. **起動テスト**
```bash
npm start
# または
node index.js
```

## 📋 完全復旧チェックリスト

### Step 1: ファイル確認 (25%)
- [ ] `package.json` の scripts.start が存在
- [ ] `index.js` が存在
- [ ] `.env` が存在し、必要なキーが設定済み

### Step 2: 依存関係 (50%)
- [ ] `npm install` 実行完了
- [ ] `discord.js`, `cheerio`, `dotenv` インストール済み

### Step 3: 動作確認 (75%)
- [ ] `npm start` でエラーなし
- [ ] "JAEN v2.1.0 ready" メッセージ表示
- [ ] Discord接続成功

### Step 4: 機能テスト (100%)
- [ ] 日本語メッセージ → 英語翻訳動作
- [ ] URL投稿 → ページ要約動作

## 🔧 よくある問題と解決法

### Q1: Discord Bot Tokenエラー
**A:** `.env`ファイルの`BOT_TOKEN_VALUE`を確認

### Q2: Google Translation APIエラー  
**A:** `.env`ファイルの`TRANSLATE_API_KEY`を確認

### Q3: Node.jsバージョン問題
**A:** Node.js v16以上を使用

### Q4: 文字化けエラー
**A:** `UTF-8`エンコーディングでファイルを保存

## 🚨 緊急時クイック復旧

```bash
# 1. プロジェクトディレクトリに移動
cd /Users/peaske/Development/jaen

# 2. 依存関係再インストール
rm -rf node_modules package-lock.json
npm install

# 3. 起動テスト
npm start

# 4. 成功メッセージ確認
# ✅ JAEN v2.1.0 ready as [BotName]
```

## 📞 サポート情報

- **バージョン:** JAEN v2.1.0
- **Node.js要件:** v16.0.0以上
- **主要機能:** 
  - 日本語→英語自動翻訳
  - URL要約機能（meta情報抽出）
  - 改行・マークダウン保持

---
*Last Updated: 2025/08/31 - Emergency Recovery Guide*